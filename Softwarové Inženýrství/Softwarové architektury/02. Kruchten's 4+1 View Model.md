# Kruchten's 4+1 View Model

> **Zkouškový fokus:** umět bezpečně popsat logical, process, development, physical view a scenarios, včetně vazby na UML diagramy.

## 1. Proč existuje 4+1 View Model

![[Pasted image 20260521232647.png]]

U větších softwarových systémů nestačí popsat architekturu jedním diagramem.
Různí stakeholdeři potřebují vidět systém z různých perspektiv.

Například:

```text
uživatele zajímá, jaké funkce systém poskytuje
vývojáře zajímá, jak je systém rozdělen do modulů a balíčků
DevOps tým zajímá, kde systém běží a jak je nasazený
architekta zajímá komunikace, závislosti, výkon, škálování a rizika
manažera zajímá rozdělení práce, náklady a rizika
```

Jedna perspektiva obvykle nestačí, protože zvýrazní jen jednu část reality.
Například class diagram může dobře popsat doménové objekty, ale neukáže deployment.
Deployment diagram ukáže servery a kontejnery, ale neukáže detailní chování use casu.

**Kruchten's 4+1 View Model** proto popisuje software architecture pomocí několika doplňujících se pohledů:

```text
1. Logical view
2. Process view
3. Development view
4. Physical view
+1. Scenarios
```

Scenarios tvoří „+1“, protože propojují ostatní čtyři pohledy a ověřují, že architektura opravdu podporuje důležité use cases.

Bezpečná zkoušková formulace:

> Kruchten's 4+1 View Model is an architectural model that describes a software system from multiple perspectives. The four main views describe functionality, runtime behavior, implementation structure and deployment. The additional scenario view connects and validates the other views using important use cases.

---

## 2. Základní idea modelu

Model není jeden konkrétní diagram.
Je to způsob, jak organizovat architektonickou dokumentaci.

Každý view odpovídá na jinou otázku:

| View | Hlavní otázka |
|---|---|
| Logical view | Co systém dělá? |
| Process view | Jak systém běží v runtime? |
| Development view | Jak je systém organizován v kódu? |
| Physical view | Kde systém běží? |
| Scenarios | Jak views spolupracují na konkrétních use cases? |

Důležité je, že views nejsou izolované.
Stejný prvek systému může být vidět v několika views.

Příklad:

```text
Order Service

Logical view:
  odpovídá za objednávky

Process view:
  zpracovává requesty, volá payment service, publikuje eventy

Development view:
  je implementován jako package/module/service v repository

Physical view:
  běží jako container v Kubernetes nebo jako část backend aplikace

Scenarios:
  účastní se scénáře "customer creates order"
```

---

## 3. Logical View

## 3.1 Co popisuje

**Logical view** se zaměřuje na funkční požadavky systému.
Popisuje, co systém dělá z pohledu domény a hlavních abstrakcí.

Řeší hlavně:

```text
hlavní doménové objekty
jejich odpovědnosti
vztahy mezi objekty
funkcionalitu systému
business logiku
klíčové abstrakce
terminologii systému
```

Typicky odpovídá na otázky:

```text
Jaké hlavní objekty nebo komponenty systém obsahuje?
Jaké jsou jejich odpovědnosti?
Jak spolu souvisí?
Jaká je doménová struktura systému?
Jaké funkce systém poskytuje uživatelům?
```

Logical view je blízký tomu, co by pochopil doménový expert nebo analytik.

---

## 3.2 Typické UML diagramy

Pro logical view se hodí hlavně:

```text
class diagram
state diagram
component diagram
object diagram
```

Nejdůležitější je obvykle **class diagram**, protože ukazuje hlavní třídy, jejich atributy, metody a vztahy.

**State diagram** se hodí, pokud má objekt důležité stavy.

Příklad pro objednávku:

```text
Created → Paid → Shipped → Delivered
Created → Cancelled
Paid → Refunded
```

---

## 3.3 Příklad logical view pro e-shop

Logical view může obsahovat například tyto doménové objekty:

```text
Customer
Product
Cart
Order
OrderItem
Payment
Shipment
Invoice
```

Vztahy:

```text
Customer creates Order
Order contains OrderItems
OrderItem references Product
Order has Payment
Order has Shipment
```

Tento pohled ještě nemusí říkat, jestli systém bude monolit, microservices nebo serverless.
Popisuje hlavně logickou doménu a funkcionalitu.

---

## 3.4 Příklad logical view pro ACG platformu

Pro platformu na kontrolu API breaking changes by logical view mohl obsahovat:

```text
ValidationArtifact
Finding
Policy
Service
Contract
Approval
Suppression
User
Role
AuditLogEntry
Notification
```

Příklad vztahů:

```text
Service owns Contract
Contract has ValidationArtifact
ValidationArtifact contains Findings
Finding can be Suppressed
Pipeline run can require Approval
User has Role
Important action creates AuditLogEntry
```

To ukazuje doménový model bez detailů deploymentu nebo konkrétní infrastruktury.

---

## 4. Process View

## 4.1 Co popisuje

**Process view** se zaměřuje na runtime chování systému.
Popisuje, jak části systému spolupracují při běhu.

Řeší hlavně nefunkční požadavky jako:

```text
performance
scalability
availability
concurrency
synchronization
communication
fault tolerance
throughput
latency
```

Typické otázky:

```text
Jaké procesy, služby nebo vlákna běží?
Jak spolu komunikují?
Je komunikace synchronní nebo asynchronní?
Kde mohou vzniknout bottlenecks?
Jak systém reaguje na vysokou zátěž?
Jak systém zvládá chyby?
```

Process view není jen business process.
Jde hlavně o runtime strukturu a dynamické chování systému.

---

## 4.2 Typické UML diagramy

Pro process view se hodí:

```text
sequence diagram
activity diagram
communication diagram
state diagram
```

**Sequence diagram** ukazuje pořadí komunikace mezi objekty nebo službami.

**Activity diagram** ukazuje tok aktivit, rozhodování, paralelizaci a workflow.

---

## 4.3 Příklad process view pro e-shop

Scénář: customer creates order.

```text
1. Customer odešle checkout request.
2. Frontend zavolá Order API.
3. Order Service validuje košík.
4. Order Service zavolá Payment Service.
5. Payment Service kontaktuje payment gateway.
6. Order Service vytvoří objednávku.
7. Order Service publikuje OrderCreated event.
8. Inventory Service sníží skladové množství.
9. Notification Service odešle potvrzení zákazníkovi.
```

Tento pohled pomáhá diskutovat například:

```text
Co se stane, když payment gateway neodpovídá?
Co se stane, když inventory update selže?
Je checkout synchronní nebo asynchronní?
Jak dlouho může zákazník čekat?
Kde má být retry?
Kde má být timeout?
```

---

## 4.4 Příklad process view pro ACG platformu

Scénář: CI/CD pipeline nahraje validation artifact.

```text
1. CLI spustí diff nástroje pro OpenAPI nebo Protobuf.
2. CLI normalizuje findings.
3. CLI vytvoří validation artifact.
4. CLI odešle artifact backendu.
5. Backend uloží metadata do databáze.
6. Backend uloží detailní artifact do object storage.
7. Backend vyhodnotí decision policy.
8. Backend vytvoří audit log.
9. Backend případně odešle Slack notification.
10. Frontend zobrazí výsledek ve validation detail page.
```

Process view zde ukazuje runtime tok mezi CLI, backendem, databází, object storage, policy engine, Slackem a frontendem.

---

## 5. Development View

## 5.1 Co popisuje

**Development view** popisuje implementační organizaci systému.
Zaměřuje se na to, jak je systém strukturován z pohledu vývojářů.

Řeší například:

```text
moduly
balíčky
vrstvy
repository strukturu
knihovny
frameworky
build systém
dependencies
team ownership
source code organization
```

Typické otázky:

```text
Jak je projekt rozdělen v kódu?
Kde se nachází business logic?
Kde jsou controllery?
Kde jsou repositories?
Jaké moduly na sobě závisí?
Jaký tým vlastní kterou část?
Jak probíhá build a deployment pipeline?
```

Development view je důležitý hlavně pro vývojáře a projektové řízení.
Pomáhá novým členům týmu rychle pochopit strukturu projektu.

---

## 5.2 UML alignment

V původním výkladu nemá development view vždy jeden přesný standardní UML diagram.
V praxi se ale často používají:

```text
package diagram
component diagram
module diagram
class diagram na vyšší úrovni
```

Důležité není vybrat „dokonalý“ diagram, ale jasně ukázat implementační strukturu.

---

## 5.3 Příklad development view pro layered backend

Typická struktura backendu:

```text
src/
  controllers/
  services/
  repositories/
  models/
  dto/
  config/
  security/
  migrations/
  tests/
```

Závislosti:

```text
Controller → Service → Repository → Database
```

Architektonické pravidlo:

```text
Controller nesmí přímo přistupovat do databáze.
Business logic nesmí být v repository.
Repository neřeší HTTP requesty.
```

---

## 5.4 Příklad development view pro microservices

V microservice architektuře může development view řešit například:

```text
order-service/
payment-service/
inventory-service/
notification-service/
api-gateway/
shared-contracts/
deployment/
```

Důležité otázky:

```text
Má každá service vlastní repository?
Nebo je použit monorepo?
Které knihovny jsou sdílené?
Jak se spravují API contracts?
Který tým vlastní kterou service?
Jak se verzují API?
```

Development view tedy neřeší jen kód, ale i organizaci vývoje.

---

## 6. Physical View

## 6.1 Co popisuje

**Physical view** popisuje deployment systému.
Řeší, kde a jak systém běží.

Zaměřuje se na:

```text
servery
kontejnery
cloud resources
network
load balancers
databáze
message brokers
object storage
firewally
external systems
runtime nodes
```

Typické otázky:

```text
Na jakých nodech systém běží?
Kolik instancí služby existuje?
Kde je databáze?
Kde je cache?
Jak je řešen load balancing?
Jak služby komunikují přes síť?
Jaké části jsou v cloudu a jaké on-premise?
```

Physical view je důležitý pro DevOps, operations, security a infrastrukturu.

---

## 6.2 Typické UML diagramy

Pro physical view se nejvíce hodí:

```text
deployment diagram
infrastructure diagram
network diagram
container diagram
```

V UML je nejtypičtější **deployment diagram**.
Ukazuje mapping softwarových komponent na fyzické nebo virtuální nody.

---

## 6.3 Příklad physical view pro e-shop

Příklad deploymentu:

```text
Client browser
  ↓ HTTPS
Load balancer
  ↓
Frontend container
  ↓ REST
Backend API containers
  ↓
PostgreSQL database
Redis cache
Message broker
External payment gateway
```

Tento pohled umožňuje řešit:

```text
kde je single point of failure
jak systém škálovat
jak řešit monitoring
jak řešit síťovou bezpečnost
jak zálohovat databázi
```

---

## 6.4 Příklad physical view pro ACG platformu

Možný physical view:

```text
CI/CD runner
  ↓
ACG CLI
  ↓ HTTPS
ACG Backend API
  ↓
PostgreSQL database
  ↓
MinIO / S3-compatible object storage
  ↓
ACG Frontend
  ↓
Slack webhook integration
```

Případně v Docker Compose:

```text
backend container
frontend container
postgres container
minio container
network
volumes
```

Physical view zde ukazuje, že metadata jsou v relační databázi, detailní artifacts v object storage a uživatelé pracují přes web UI.

---

## 7. Scenarios View (+1)

## 7.1 Co popisuje

**Scenarios** jsou konkrétní use cases nebo user tasks, které ukazují, jak systém funguje napříč ostatními views.

Scenarios tvoří „+1“, protože:

```text
ověřují architekturu
propojují logical, process, development a physical view
ukazují chování systému na konkrétních případech
pomáhají odhalit chybějící části návrhu
pomáhají komunikovat se stakeholdery
```

Scenarios mohou být:

```text
hlavní business use cases
kritické quality attribute scenarios
rizikové scénáře
failure scenarios
performance scenarios
security scenarios
```

---

## 7.2 Proč jsou scenarios důležité

Bez scenarios může být architektura jen statický popis.
Scenarios ukazují, jestli statická struktura opravdu funguje pro reálné požadavky.

Příklad:

```text
Máme class diagram, deployment diagram a module diagram.
Ale dokud neprojdeme scénář "customer creates order",
nevíme, jestli všechny části opravdu spolupracují správně.
```

Scenarios tedy slouží jako validace architektury.

---

## 7.3 Příklad scenario: customer creates order

Scenario:

```text
Customer creates an order and pays online.
```

Logical view:

```text
Customer, Cart, Order, Payment, Product
```

Process view:

```text
Frontend → Order Service → Payment Service → Payment Gateway
Order Service → Inventory Service
Order Service → Notification Service
```

Development view:

```text
order module
payment module
inventory module
notification module
API clients
DTOs
repositories
```

Physical view:

```text
frontend container
backend service containers
PostgreSQL
message broker
external payment gateway
```

Tím jeden scénář propojí všechny views.

---

## 7.4 Příklad scenario: validation artifact upload

Scenario:

```text
CI/CD pipeline uploads validation artifact and receives decision.
```

Logical view:

```text
ValidationArtifact
Finding
Policy
Decision
Service
Contract
```

Process view:

```text
CLI → Backend API → Policy evaluation → Database/Object Storage → Notification
```

Development view:

```text
cli package
artifact service
policy service
repository layer
notification module
frontend validation detail page
```

Physical view:

```text
CI runner
backend container
PostgreSQL
MinIO
frontend container
Slack webhook
```

Výsledek:

```text
Scénář ukazuje, že architektura podporuje CI/CD integraci,
storage artifacts, policy evaluation, auditability a user review.
```

---

## 8. UML alignment

Kruchten's 4+1 není totéž co UML.
Je to model pohledů na architekturu.
UML se ale často používá jako jazyk pro vyjádření jednotlivých views.

| Kruchten view | Co popisuje | Typické UML diagramy |
|---|---|---|
| Logical view | Funkcionalita, doménové objekty, vztahy | Class diagram, State diagram, Object diagram |
| Process view | Runtime chování, komunikace, concurrency, performance | Sequence diagram, Activity diagram, Communication diagram |
| Development view | Implementační struktura, moduly, balíčky | Package diagram, Component diagram |
| Physical view | Deployment, nodes, infrastructure | Deployment diagram |
| Scenarios | Use cases propojující views | Use-case diagram, Sequence diagram, Activity diagram |

Důležité:

```text
UML diagramy jsou nástroje.
4+1 View Model je organizační rámec pro architektonickou dokumentaci.
```

---

## 9. Jak model zachycuje complete behavior systému

4+1 model zachycuje systém z více pohledů, protože komplexní systém nelze plně pochopit jen jedním pohledem.

### Logical view

Ukazuje, co systém dělá a jaké má hlavní doménové koncepty.

### Process view

Ukazuje, jak systém funguje při běhu, jak komponenty komunikují a jak systém splňuje runtime quality attributes.

### Development view

Ukazuje, jak je systém implementačně organizován, aby ho vývojáři mohli vyvíjet, udržovat a rozšiřovat.

### Physical view

Ukazuje, jak je systém nasazený na infrastruktuře.

### Scenarios

Ukazují konkrétní průchody systémem a ověřují, že všechny views dohromady dávají smysl.

Proto lze říct:

> The 4+1 model captures the complete architecture by combining static structure, dynamic behavior, implementation organization, deployment structure and concrete scenarios that validate the architecture.

---

## 10. Časté chyby u zkoušky

### 10.1 Splést process view s business processem

Process view není jen obchodní proces.
Je to runtime pohled na procesy, komunikaci, concurrency, performance a scalability.

Špatně:

```text
Process view is only BPMN workflow.
```

Lépe:

```text
Process view describes runtime behavior, processes, communication and non-functional properties such as performance and availability.
```

---

### 10.2 Brát scenarios jako obyčejný pátý diagram

Scenarios nejsou jen další diagram vedle ostatních.
Jsou to konkrétní use cases, které propojují a validují ostatní views.

---

### 10.3 Myslet si, že každý view musí mít přesně jeden UML diagram

Nemusí.
Model říká, jaké perspektivy popsat.
UML je jen jeden z možných způsobů zápisu.

---

### 10.4 Ignorovat stakeholdery

Každý view je užitečný pro jiné stakeholdery:

```text
Logical view → analyst, domain expert, user-oriented stakeholders
Process view → architect, performance engineer, backend developer
Development view → developers, team leads, project managers
Physical view → DevOps, operations, security
Scenarios → všichni, protože ukazují reálné chování systému
```

---

## 11. Mini příklad: jeden systém ve všech views

Systém: online ticket reservation.

### Logical view

```text
User
Concert
Ticket
Reservation
Payment
Venue
```

### Process view

```text
User searches concert.
System checks available tickets.
User reserves ticket.
System temporarily locks seat.
User pays.
System confirms reservation.
System sends email.
```

### Development view

```text
frontend/
backend/
  concert-module/
  reservation-module/
  payment-module/
  notification-module/
database/
deployment/
```

### Physical view

```text
Browser
Load balancer
Frontend server
Backend API
PostgreSQL
Redis lock/cache
External payment gateway
Email provider
```

### Scenarios

```text
User successfully buys ticket.
Payment fails.
Two users try to reserve the same seat.
External payment gateway is unavailable.
System is under high load before popular concert.
```

Tento příklad ukazuje, že scénáře pomáhají testovat nejen funkcionalitu, ale i availability, consistency, performance a fault handling.

---

## 12. Vztah k quality attributes

4+1 model není jen dokumentační šablona.
Pomáhá také uvažovat o kvalitě architektury.

Příklad:

```text
Performance:
  nejvíce se projeví v process view a physical view

Maintainability:
  nejvíce se projeví v development view a logical view

Security:
  projeví se ve všech views

Scalability:
  process view + physical view

Testability:
  development view + scenarios

Usability:
  logical view + scenarios
```

Scenarios jsou zvlášť důležité pro quality attributes, protože kvalita musí být ověřitelná na konkrétních situacích.

Příklad quality scenario:

```text
Stimulus:
  1000 users try to buy tickets within one minute.

Environment:
  system is under high load.

Artifact:
  reservation service and database.

Response:
  system accepts valid requests and prevents double booking.

Response measure:
  95 % requests finish under 2 seconds, no seat is sold twice.
```

---

## 13. Zkoušková odpověď: Describe Kruchten's 4+1 View Model

Kruchten's 4+1 View Model is a model for describing software architecture from multiple perspectives. It consists of four main views and one additional scenario view. The purpose is to capture the architecture of a complex system more completely than a single diagram could.

The **logical view** describes the functionality of the system and its main domain concepts. It focuses on objects, classes, relationships and functional requirements. It can be represented by UML class diagrams or state diagrams.

The **process view** describes the runtime behavior of the system. It focuses on processes, communication, concurrency, synchronization, performance, scalability and availability. It can be represented by sequence diagrams or activity diagrams.

The **development view** describes the implementation structure of the system. It focuses on modules, packages, components, layers, libraries, dependencies and the organization of the source code. It is important for developers and project management.

The **physical view** describes how the system is deployed. It maps software elements to hardware, virtual machines, containers, network nodes or cloud infrastructure. It can be represented by UML deployment diagrams.

The **scenario view** represents important use cases or user tasks. Scenarios connect the other four views and validate that the architecture supports real system behavior. They show how logical elements, runtime processes, implementation modules and physical nodes work together.

The model is aligned with UML because each view can be expressed using suitable UML diagrams. For example, logical view can use class diagrams, process view can use sequence and activity diagrams, physical view can use deployment diagrams, and scenarios can use use-case or sequence diagrams. UML is the notation, while 4+1 is the architectural organization of views.

---

## 14. Krátké shrnutí

```text
Kruchten's 4+1 View Model:

Logical view:
  co systém dělá
  funkce, objekty, doména
  class/state diagrams

Process view:
  jak systém běží
  runtime behavior, communication, concurrency, performance
  sequence/activity diagrams

Development view:
  jak je systém organizován v kódu
  modules, packages, layers, libraries, dependencies
  package/component diagrams

Physical view:
  kde systém běží
  deployment, nodes, servers, containers, infrastructure
  deployment diagram

Scenarios:
  konkrétní use cases
  propojují a validují ostatní views
  ukazují complete behavior systému
```

Nejdůležitější věta:

> 4+1 View Model helps describe software architecture from different perspectives and uses scenarios to show how these perspectives work together in real use cases.
