### Kapitoly

1. [[1. Úvod, hardware a motivace|Úvod, hardware a motivace]]
   - [[1. Úvod, hardware a motivace#Motivace a definice efektivního softwaru|Motivace a definice efektivního softwaru]] — co vlastně znamená "efektivní" a proč na tom záleží.
   - [[1. Úvod, hardware a motivace#Výkon jako trade-off|Výkon jako trade-off]] — proč rychlost není důležitější než správnost, bezpečnost a udržovatelnost.
   - [[1. Úvod, hardware a motivace#Optimalizační přístup a technologický stack|Optimalizační přístup a technologický stack]] — iterativní cyklus měř–rozuměj–opravuj–ověř.
   - [[1. Úvod, hardware a motivace#Benchmarking vs profiling|Benchmarking vs profiling]] — rozdíl mezi měřením rychlosti a hledáním příčiny problému.
   - [[1. Úvod, hardware a motivace#Hardware: procesory a paměťová hierarchie|Hardware: procesory a paměťová hierarchie]] — jak CPU, cache a DRAM spolupracují a kde je bottleneck.
   - [[1. Úvod, hardware a motivace#Locality a working set|Locality a working set]] — spatial/temporal locality a proč cache závisí na velikosti aktivních dat.
   - [[1. Úvod, hardware a motivace#Instruction-Level Parallelism (ILP)|Instruction-Level Parallelism (ILP)]] — superskalar, pipeline, out-of-order execution, branch prediction.
   - [[1. Úvod, hardware a motivace#Task parallelism a vlákna|Task parallelism a vlákna]] — kdy přidání vláken pomůže a kdy ne.
2. [[2. Bentleyova pravidla pro efektivní kód|Bentleyova pravidla pro efektivní kód]]
   - [[2. Bentleyova pravidla pro efektivní kód#Základní pravidlo měř a redukuj práci|Základní pravidlo měř a redukuj práci]] — optimalizace začíná měřením a největší efekt mívá odstranění práce.
   - [[2. Bentleyova pravidla pro efektivní kód#Optimalizace datových struktur|Optimalizace datových struktur]] — Reduce Bits, Augmentation, Precomputation, Sparsity/Caching.
   - [[2. Bentleyova pravidla pro efektivní kód#Optimalizace logiky|Optimalizace logiky]] — short-circuit, reordering conditions, loop unswitching.
   - [[2. Bentleyova pravidla pro efektivní kód#Optimalizace smyček|Optimalizace smyček]] — invariant extraction, loop unrolling, fusion/fission, strength reduction, tiling/blocking.
   - [[2. Bentleyova pravidla pro efektivní kód#Optimalizace funkcí|Optimalizace funkcí]] — inlining, tail call, coarsening, allocation hot path, specialization.
   - [[2. Bentleyova pravidla pro efektivní kód#Praktické optimalizace mimo CPU|Praktické optimalizace mimo CPU]] — batching, I/O round trips a omezení sdíleného stavu.
   - [[2. Bentleyova pravidla pro efektivní kód#Anti-patterny a checklist|Anti-patterny a checklist]] — premature optimization, micro-tuning bez profilu a kontrola trade-offů.
3. [[3. Překladač, LLVM a profile-guided optimization|Překladač, LLVM a profile-guided optimization]]
   - [[3. Překladač, LLVM a profile-guided optimization#Proč potřebujeme compiler mental model|Proč potřebujeme compiler mental model]] — proč zdrojový kód není to, co vykonává CPU.
   - [[3. Překladač, LLVM a profile-guided optimization#Compiler pipeline|Compiler pipeline]] — frontend, AST, IR, optimizer, backend a machine code.
   - [[3. Překladač, LLVM a profile-guided optimization#AST, IR, CFG a SSA|AST, IR, CFG a SSA]] — reprezentace programu pro analýzu a optimalizace.
   - [[3. Překladač, LLVM a profile-guided optimization#LLVM jako konkrétní implementace pipeline|LLVM jako konkrétní implementace pipeline]] — stejná compiler pipeline, jen v terminologii LLVM.
   - [[3. Překladač, LLVM a profile-guided optimization#Optimalizační průchody|Optimalizační průchody]] — high-level a low-level passes, které compiler dělá automaticky.
   - [[3. Překladač, LLVM a profile-guided optimization#Limity překladače|Limity překladače]] — aliasing, side effects, runtime data, virtual dispatch a hranice modulů.
   - [[3. Překladač, LLVM a profile-guided optimization#Compiler flags a build režimy|Compiler flags a build režimy]] — `-O0`, `-O2`, `-O3`, `-Ofast`, LTO a debug vs. release.
   - [[3. Překladač, LLVM a profile-guided optimization#PGO, JIT a profiling|PGO, JIT a profiling]] — profilovací data, hot path, hardware counters a flame graph.
   - [[3. Překladač, LLVM a profile-guided optimization#Časté chyby v myšlení|Časté chyby v myšlení]] — proč compiler není magie a proč `-O3` nemusí být rychlejší než `-O2`.
   - [[3. Překladač, LLVM a profile-guided optimization#Zkouškové minimum|Zkouškové minimum]] — krátké odpovědi a tahák ke státnicím.
4. [[4. Benchmarking a měření výkonu|Benchmarking a měření výkonu]]
   - [[4. Benchmarking a měření výkonu#Proč je benchmarking těžký|Proč je benchmarking těžký]] — program neběží izolovaně a jeden běh nestačí.
   - [[4. Benchmarking a měření výkonu#Benchmarking vs profiling|Benchmarking vs profiling]] — benchmark měří výsledek, profiling hledá příčinu.
   - [[4. Benchmarking a měření výkonu#Co vlastně měřit|Co vlastně měřit]] — latency, throughput, CPU/wall-clock time, paměť, alokace a energie.
   - [[4. Benchmarking a měření výkonu#Typy benchmarků|Typy benchmarků]] — microbenchmark, application benchmark a load test.
   - [[4. Benchmarking a měření výkonu#Návrh správného experimentu|Návrh správného experimentu]] — baseline, reprezentativní data, warm-up, iterace a runs.
   - [[4. Benchmarking a měření výkonu#Measurement noise|Measurement noise]] — CPU frequency, scheduler, GC, JIT, cache, NUMA a virtualizace.
   - [[4. Benchmarking a měření výkonu#JVM benchmarking|JVM benchmarking]] — warm-up, JIT optimalizace, dead code elimination a JMH.
   - [[4. Benchmarking a měření výkonu#C-C++ benchmarking|C/C++ benchmarking]] — release build, compiler optimizations, Google Benchmark a perf.
   - [[4. Benchmarking a měření výkonu#Hardware performance counters|Hardware performance counters]] — cycles, instructions, IPC, cache misses, branch misses a perf workflow.
   - [[4. Benchmarking a měření výkonu#Statistická interpretace výsledků|Statistická interpretace výsledků]] — mean, median, percentily, variance, confidence interval a speedup.
   - [[4. Benchmarking a měření výkonu#Amdahlova intuice|Amdahlova intuice]] — proč optimalizace malé části programu nezrychlí celý systém dramaticky.
   - [[4. Benchmarking a měření výkonu#Load testing a tail latency|Load testing a tail latency]] — open-loop/closed-loop load, saturation point, queueing a p99.
   - [[4. Benchmarking a měření výkonu#Benchmarking checklist|Benchmarking checklist]] — co ověřit před měřením, během měření a po něm.
   - [[4. Benchmarking a měření výkonu#Zkouškové minimum|Zkouškové minimum]] — krátké odpovědi a tahák ke státnicím.
5. [[5. Synchronizace a RCU (C-C++)|Synchronizace a RCU (C/C++)]]
   - [[5. Synchronizace a RCU (C-C++)#Proč synchronizace existuje|Proč synchronizace existuje]] — sdílený stav, race condition, kritická sekce a cíle synchronizace.
   - [[5. Synchronizace a RCU (C-C++)#Atomic operations a memory ordering|Atomic operations a memory ordering]] — atomics, CAS, ABA problém a acquire/release.
   - [[5. Synchronizace a RCU (C-C++)#Mutex a RAII|Mutex a RAII]] — vzájemné vyloučení, futexová rychlá cesta a bezpečné odemykání v C++.
   - [[5. Synchronizace a RCU (C-C++)#Deadlock, priority inversion a convoy effect|Deadlock, priority inversion a convoy effect]] — typické systémové problémy okolo locků.
   - [[5. Synchronizace a RCU (C-C++)#Condition variable a semaphore|Condition variable a semaphore]] — čekání na událost a řízení omezeného počtu zdrojů.
   - [[5. Synchronizace a RCU (C-C++)#Read-write lock|Read-write lock]] — paralelní čtení, exkluzivní zápis a limity rw-locku.
   - [[5. Synchronizace a RCU (C-C++)#Spinlock a futex|Spinlock a futex]] — aktivní čekání vs. uspání vlákna v kernelu.
   - [[5. Synchronizace a RCU (C-C++)#Read-Copy-Update (RCU)|Read-Copy-Update (RCU)]] — read-mostly workload, copy-on-write, grace period a bezpečné uvolnění starých verzí.
   - [[5. Synchronizace a RCU (C-C++)#Copy-on-write|Copy-on-write]] — snapshot pattern, výhody, nevýhody a rozdíl proti RCU.
   - [[5. Synchronizace a RCU (C-C++)#Jak vybrat synchronizační mechanismus|Jak vybrat synchronizační mechanismus]] — praktická volba podle workloadu a ceny kritické sekce.
   - [[5. Synchronizace a RCU (C-C++)#Zkouškové minimum|Zkouškové minimum]] — krátké odpovědi a tahák ke státnicím.
6. [[6. Cache, paměť a cache-efektivní algoritmy|Cache, paměť a cache-efektivní algoritmy]]
   - [[6. Cache, paměť a cache-efektivní algoritmy#Proč cache vůbec existuje|Proč cache vůbec existuje]] — CPU je rychlé, DRAM je pomalá a cache zakrývá rozdíl v latenci.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Memory hierarchy|Memory hierarchy]] — registry, L1/L2/L3 cache, DRAM a trade-off rychlosti/kapacity.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Cache line, locality a working set|Cache line, locality a working set]] — 64B cache line, spatial/temporal locality a aktivní data.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Cache hit, cache miss a typy missů|Cache hit, cache miss a typy missů]] — cold, capacity, conflict, true sharing a false sharing miss.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Cache organization|Cache organization]] — direct-mapped, set-associative cache a tag/set/offset.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Sequential vs random access|Sequential vs random access]] — proč pole bývá rychlejší než linked list.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Data layout|Data layout]] — AoS vs. SoA, pointer chasing, hot/cold data a dynamická alokace.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Cache-efficient matrix multiplication|Cache-efficient matrix multiplication]] — row-major layout, loop order a blocking/tiling.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Write policies a streaming writes|Write policies a streaming writes]] — write-through, write-back, write-combining a streaming stores.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Multi-core cache, MESI a false sharing|Multi-core cache, MESI a false sharing]] — coherency, true sharing, false sharing a cache line ping-pong.
   - [[6. Cache, paměť a cache-efektivní algoritmy#NUMA|NUMA]] — lokální vs. vzdálená paměť, first-touch policy a thread pinning.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Instruction cache a self-evicting code|Instruction cache a self-evicting code]] — proč může být problémem i příliš velký hot code.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Jak cache souvisí se synchronizací a JVM|Jak cache souvisí se synchronizací a JVM]] — atomics, locky, object layout, TLAB a GC.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Profiling cache problémů|Profiling cache problémů]] — perf, IPC, stalls, cache misses a experimenty.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Cache-friendly pravidla|Cache-friendly pravidla]] — praktická pravidla pro layout, průchod daty a měření.
   - [[6. Cache, paměť a cache-efektivní algoritmy#Zkouškové minimum|Zkouškové minimum]] — krátké odpovědi a tahák ke státnicím.
7. [[7. Serializace dat a RPC|Serializace dat a RPC]]
   - [[7. Serializace dat a RPC#Co je serializace|Co je serializace]] — objekt v paměti, bajty, transport a objekt v jiném procesu.
   - [[7. Serializace dat a RPC#Proč serializace ovlivňuje výkon|Proč serializace ovlivňuje výkon]] — CPU cost, alokace, payload size, kompatibilita a latency budget.
   - [[7. Serializace dat a RPC#Textové vs binární formáty|Textové vs binární formáty]] — čitelnost vs. velikost, parsing, typy a schema.
   - [[7. Serializace dat a RPC#JSON a XML|JSON a XML]] — kdy dávají smysl a proč jsou drahé v high-throughput systémech.
   - [[7. Serializace dat a RPC#Binární formáty|Binární formáty]] — Protobuf, Avro, MessagePack, FlatBuffers a Cap'n Proto.
   - [[7. Serializace dat a RPC#Schema evolution a compatibility|Schema evolution a compatibility]] — backward/forward/full compatibility, breaking changes a unknown fields.
   - [[7. Serializace dat a RPC#RPC|RPC]] — remote call pipeline, transparentnost, timeouty, retry a CORBA jako historická poznámka.
   - [[7. Serializace dat a RPC#REST a gRPC|REST a gRPC]] — REST/JSON vs. gRPC/Protobuf, HTTP/1.1 vs. HTTP/2 a streaming.
   - [[7. Serializace dat a RPC#Payload size, compression a fetching|Payload size, compression a fetching]] — base64 overhead, over-fetching, under-fetching a kdy komprimovat.
   - [[7. Serializace dat a RPC#Zero-copy a buffer-oriented parsing|Zero-copy a buffer-oriented parsing]] — lazy parsing, přímé čtení z bufferu a limity zero-copy.
   - [[7. Serializace dat a RPC#Batching, streaming a backpressure|Batching, streaming a backpressure]] — jak snížit počet RPC a neshodit příjemce.
   - [[7. Serializace dat a RPC#Security considerations|Security considerations]] — deserialization attacks, resource limits a bezpečné parsery.
   - [[7. Serializace dat a RPC#Praktická volba formátu|Praktická volba formátu]] — jak vybrat formát podle situace.
   - [[7. Serializace dat a RPC#Zkouškové minimum|Zkouškové minimum]] — krátké odpovědi a tahák ke státnicím.
8. [[8. JVM Internals — bytecode, JIT a safepoints|JVM Internals — bytecode, JIT a safepoints]]
   - [[8. JVM Internals — bytecode, JIT a safepoints#JVM jako managed runtime|JVM jako managed runtime]] — class loading, bytecode verification, interpreter, JIT, GC a runtime services.
   - [[8. JVM Internals — bytecode, JIT a safepoints#JVM memory layout|JVM memory layout]] — PC, JVM stack, frame, heap, metaspace a code cache.
   - [[8. JVM Internals — bytecode, JIT a safepoints#JVM bytecode|JVM bytecode]] — `.class`, constant pool, stack-oriented instrukce, `javap` a decompiler.
   - [[8. JVM Internals — bytecode, JIT a safepoints#Object layout, OOP a compressed OOP|Object layout, OOP a compressed OOP]] — object header, reference, padding a paměťový overhead objektů.
   - [[8. JVM Internals — bytecode, JIT a safepoints#JIT, tiered compilation a optimalizace|JIT, tiered compilation a optimalizace]] — interpreter, C1/C2, profiling, inlining, escape analysis, OSR a deoptimization.
   - [[8. JVM Internals — bytecode, JIT a safepoints#Safepoints|Safepoints]] — OopMap, globální safepoint, thread-local handshake, TTSP a stop-the-world.
   - [[8. JVM Internals — bytecode, JIT a safepoints#Profiling JVM aplikací|Profiling JVM aplikací]] — typy profilování, CPU/allocation/heap/GC/lock profiling, warm-up, JMH, JFR a async-profiler.
9. [[9. Data races a synchronizace v JVM|Data races a synchronizace v JVM]]
   - [[9. Data races a synchronizace v JVM#Proč je multi-threading těžký|Proč je multi-threading těžký]] — atomicity, visibility, ordering a proč single-thread intuice nestačí.
   - [[9. Data races a synchronizace v JVM#Data race a Java Memory Model|Data race a Java Memory Model]] — data race, happens-before, safe publication a pravidla JMM.
   - [[9. Data races a synchronizace v JVM#Memory barriers a volatile|Memory barriers a volatile]] — acquire/release, memory fences, stop flag a limity `volatile`.
   - [[9. Data races a synchronizace v JVM#Synchronized, monitory a JVM locking|Synchronized, monitory a JVM locking]] — `monitorenter`, `monitorexit`, thin/fat locking, lock inflation a `wait/notify`.
   - [[9. Data races a synchronizace v JVM#ReentrantLock, Conditions a read-mostly workload|ReentrantLock, Conditions a read-mostly workload]] — explicitní locky, fair/unfair režim, `ReadWriteLock`, `StampedLock` a immutable snapshot.
   - [[9. Data races a synchronizace v JVM#Atomics, CAS a LongAdder|Atomics, CAS a LongAdder]] — Atomic classes, field updaters, VarHandle, CAS loop a high-contention countery.
   - [[9. Data races a synchronizace v JVM#Non-blocking algoritmy|Non-blocking algoritmy]] — blocking, obstruction-free, lock-free, wait-free, linearizability, Treiber stack a ABA.
   - [[9. Data races a synchronizace v JVM#Performance bottlenecks synchronizace|Performance bottlenecks synchronizace]] — lock contention, CAS contention, false sharing, blocking vs. spinning a měření.
10. [[10. Síťová komunikace a C10K problém|Síťová komunikace a C10K problém]]
    - [[10. Síťová komunikace a C10K problém#Networking mental model|Networking mental model]] — latency, throughput, concurrent connections, sockety, buffery a proč síťový server často čeká.
    - [[10. Síťová komunikace a C10K problém#OSI model a TCP/IP stack|OSI model a TCP/IP stack]] — vrstvy, TCP/UDP, socket 4-tuple, connection setup a head-of-line blocking.
    - [[10. Síťová komunikace a C10K problém#Blocking I/O a C10K problém|Blocking I/O a C10K problém]] — thread-per-connection, thread-per-request, stack memory, context switching a scheduler overhead.
    - [[10. Síťová komunikace a C10K problém#Non-blocking I/O a readiness notification|Non-blocking I/O a readiness notification]] — `O_NONBLOCK`, `select`, `poll`, `epoll`, `kqueue`, level-triggered vs. edge-triggered.
    - [[10. Síťová komunikace a C10K problém#Event-driven server a Reactor pattern|Event-driven server a Reactor pattern]] — event loop, reactor, worker pool, backpressure a proč handler nesmí blokovat.
    - [[10. Síťová komunikace a C10K problém#Java IO a Java NIO|Java IO a Java NIO]] — `Socket`, `SocketChannel`, `ServerSocketChannel`, `Selector`, `SelectionKey` a NIO server skeleton.
    - [[10. Síťová komunikace a C10K problém#ByteBuffer, direct buffers a zero-copy|ByteBuffer, direct buffers a zero-copy]] — heap vs. direct buffer, `flip/clear/compact`, pooling, `transferTo` a limity zero-copy.
    - [[10. Síťová komunikace a C10K problém#Thread pools a virtual threads|Thread pools a virtual threads]] — bounded pools, overload, Java virtual threads, carrier threads a kdy virtual threads nepomůžou.
    - [[10. Síťová komunikace a C10K problém#Performance bottlenecks síťových serverů|Performance bottlenecks síťových serverů]] — CPU, memory, kernel, backend, timeouts, retries, monitoring a časté chyby.
11. [[11. Paměť JVM, datové struktury a kolekce|Paměť JVM, datové struktury a kolekce]]
    - [[11. Paměť JVM, datové struktury a kolekce#Proč řešit paměť v Javě|Proč řešit paměť v Javě]] — Java skrývá layout, ale výkon závisí na objektech, referencích, alokacích a cache.
    - [[11. Paměť JVM, datové struktury a kolekce#Memory analysis workflow|Memory analysis workflow]] — object histogram, heap dump, allocation profiling, dominator tree a paths to GC roots.
    - [[11. Paměť JVM, datové struktury a kolekce#Shallow size, retained size a dominator tree|Shallow size, retained size a dominator tree]] — jak poznat, co opravdu drží paměť.
    - [[11. Paměť JVM, datové struktury a kolekce#Primitiva, objekty, reference a arrays|Primitiva, objekty, reference a arrays]] — object header, compressed OOP, primitive array vs. object array.
    - [[11. Paměť JVM, datové struktury a kolekce#Autoboxing a boxing overhead|Autoboxing a List<Integer> vs int[]]] — proč boxed typy zvyšují paměť, alokace, GC pressure a cache miss.
    - [[11. Paměť JVM, datové struktury a kolekce#Java kolekce a jejich overhead|Java kolekce a jejich overhead]] — `ArrayList`, `LinkedList`, `HashMap`, load factor, initial capacity a resizing.
    - [[11. Paměť JVM, datové struktury a kolekce#Performance-oriented kolekce|Performance-oriented kolekce]] — type-specific kolekce, open addressing, data locality a kdy se vyplatí.
    - [[11. Paměť JVM, datové struktury a kolekce#Časté memory leaky v Javě|Časté memory leaky v Javě]] — static cache, `ThreadLocal`, listenery, neomezené mapy a jak leak potvrdit.
12. [[12. Alokace objektů, Bloom filtry a typy referencí|Alokace objektů, Bloom filtry a typy referencí]]
    - [[12. Alokace objektů, Bloom filtry a typy referencí#JVM alokace objektů a TLAB|JVM alokace objektů a TLAB]] — heap, Eden, bump-the-pointer, TLAB refill, slow path a proč je běžná alokace rychlá.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Object initialization a velké objekty|Object initialization a velké objekty]] — nulování paměti, constructor, safe publication a G1 humongous objects.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Escape analysis a scalar replacement|Escape analysis a scalar replacement]] — kdy JIT odstraní alokaci, rozloží objekt na skalární hodnoty a odstraní zámek.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Data locality a NUMA|Data locality a NUMA]] — proč krátkodobá locality pomáhá, proč object graph škodí cache a kdy řešit NUMA.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Bloom filter|Bloom filter]] — kompaktní membership test, false positives, parametry `m/n/k`, saturation a kdy ho použít.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Bloom filter varianty|Bloom filter varianty]] — counting, scalable, blocked, stable Bloom filter a Cuckoo filter.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Java reference types|Java reference types]] — strong, soft, weak, phantom reachability, `ReferenceQueue`, `WeakHashMap` a `Cleaner`.
    - [[12. Alokace objektů, Bloom filtry a typy referencí#Časté chyby|Časté chyby]] — object pooling malých objektů, SoftReference cache, mazání z Bloom filteru a reference pasti.
13. [[13. Garbage Collectors v JVM|Garbage Collectors v JVM]]
    - [[13. Garbage Collectors v JVM#Základy automatické správy paměti|Základy automatické správy paměti]] — GC roots, reachability, stop-the-world vs. concurrent.
    - [[13. Garbage Collectors v JVM#Generational hypothesis a organizace heapu|Generational hypothesis a organizace heapu]] — Young/Old gen, Eden, Survivor spaces.
    - [[13. Garbage Collectors v JVM#Design dimensions GC|Design dimensions GC]] — mark-sweep, mark-compact, copying, concurrent, incremental.
    - [[13. Garbage Collectors v JVM#Parallel collector|Parallel collector]] — Remembered Set, Parallel Scavenge, Parallel Mark-Compact.
    - [[13. Garbage Collectors v JVM#Garbage-First (G1) collector|Garbage-First (G1) collector]] — region-based heap, young-only/mixed/full collections, humongous objects.
14. [[14. Virtualizace|Virtualizace]]
    - [[14. Virtualizace#Historie a motivace virtualizace|Historie a motivace virtualizace]] — proč vůbec virtualizovat a co tím získáme.
    - [[14. Virtualizace#Základy virtualizace|Základy virtualizace]] — VMM/hypervisor, typ 1 vs. typ 2, privilege rings.
    - [[14. Virtualizace#Hardware-asistovaná virtualizace|Hardware-asistovaná virtualizace]] — Intel VT-x, AMD-V, nested paging (EPT).
    - [[14. Virtualizace#I/O virtualizace|I/O virtualizace]] — NIC emulace, Virtio/paravirtualizace, PCI pass-through, SR-IOV.
    - [[14. Virtualizace#Efektivní inter-VM networking|Efektivní inter-VM networking]] — hardware offloading, vSwitch, DPDK.
15. [[15. Shrnutí pro státnice|Shrnutí pro státnice]] ← **začni zde před zkouškou**

## Státnicové minimum (ESW)

- **JVM internals:** paměťové rozložení, OOP/compressed OOP, JVM bytecode, JIT (C1/C2, tiered compilation, OSR), safepoints, GC (generational hypothesis, Parallel, G1), profiling (sampling vs. tracing, warm-up).
- **Synchronizace v JVM:** data races, memory barriers, volatile, Java Memory Model, thin/fat/biased locking, CAS operace, atomic field updaters, non-blocking algoritmy (LIFO stack, ConcurrentHashMap).
- **Paměť a datové struktury v JVM:** statická/dynamická analýza, shallow vs. retained size, autoboxing overhead, performance kolekce, open addressing hashing, Bloom filtry, weak/soft/phantom reference.
- **Alokace v JVM:** TLAB (bump-the-pointer), NUMA alignment, escape analysis.
- **Síťová komunikace:** OSI model, C10K problém, blocking vs. non-blocking I/O, select/poll/epoll, Java NIO (ByteBuffer, Channel, Selector), virtual threads.
- **Synchronizace v C/C++:** mutex, semaphore, rw-lock, spinlock — kdy co, jaký overhead. Read-mostly workload, RCU — výhody a nevýhody.
- **Cache-efektivní algoritmy:** SRAM vs. DRAM, typy cache miss, false sharing, MESI protokol, NUMA, cache-oblivious algoritmy, tiled matrix multiplication.
- **Profiling a optimalizace v C/C++:** hardware performance counters (perf), profile-guided optimization (PGO), struktura překladače, LLVM IR, optimalizační průchody.

---
[[Všechny widgety]]
